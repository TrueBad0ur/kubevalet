name: Release Helm chart

on:
  push:
    tags:
      - "chart-v*"   # trigger: git tag chart-v0.3.3 && git push --tags

env:
  REGISTRY: ghcr.io
  CHART_NAME: kubevalet
  CHART_PATH: charts/kubevalet

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write   # needed to push to ghcr.io

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Helm
        uses: azure/setup-helm@v4

      - name: Install oras (for pushing artifacthub-repo.yml)
        uses: oras-project/setup-oras@v1

      - name: Extract chart version and lowercase owner
        id: meta
        run: |
          echo "version=${GITHUB_REF_NAME#chart-v}" >> "$GITHUB_OUTPUT"
          echo "owner=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

      - name: Lint chart
        run: helm lint ${{ env.CHART_PATH }}

      - name: Log in to ghcr.io
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | \
          helm registry login ${{ env.REGISTRY }} \
            --username ${{ steps.meta.outputs.owner }} \
            --password-stdin

      - name: Log in to ghcr.io (oras)
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | \
          oras login ${{ env.REGISTRY }} \
            --username ${{ steps.meta.outputs.owner }} \
            --password-stdin

      - name: Package chart
        run: |
          helm package ${{ env.CHART_PATH }} \
            --version ${{ steps.meta.outputs.version }} \
            --app-version ${{ steps.meta.outputs.version }} \
            --destination /tmp/chart-output

      - name: Push chart to ghcr.io
        run: |
          helm push \
            /tmp/chart-output/${{ env.CHART_NAME }}-${{ steps.meta.outputs.version }}.tgz \
            oci://${{ env.REGISTRY }}/${{ steps.meta.outputs.owner }}

      - name: Push artifacthub-repo.yml to ghcr.io
        run: |
          oras push \
            ${{ env.REGISTRY }}/${{ steps.meta.outputs.owner }}/${{ env.CHART_NAME }}:artifacthub.io \
            --config /dev/null:application/vnd.cncf.artifacthub.config.v1+yaml \
            artifacthub-repo.yml:application/vnd.cncf.artifacthub.repository-metadata.layer.v1.yaml
