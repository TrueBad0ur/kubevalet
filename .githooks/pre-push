#!/bin/sh
# Prevents direct push to main — use PRs instead.

branch=$(git symbolic-ref --short HEAD 2>/dev/null)

if [ "$branch" = "main" ]; then
  echo ""
  echo "  pre-push: direct push to main is not allowed."
  echo "  Open a PR and squash merge instead."
  echo ""
  exit 1
fi

exit 0
